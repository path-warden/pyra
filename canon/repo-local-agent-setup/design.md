---
schema_version: 1
id: OKF-3FKN7JQ8CB3K
type: design
---

# Design Document

## Context

Pyra repository enablement is currently split between `pyra init` and a
source-checkout-only `install_skills.sh` that copies global skills and supports
only part of the agent ecosystem. The approved requirement artifact
OKF-DEBA7FA1Z6A5 replaces that split path with repository-local instructions,
multi-tool MCP configuration, and local enforcement generated by `pyra init`.

## User Need

Repository maintainers need one safe, repeatable command that makes Pyra's
authority lifecycle discoverable and usable by any selected coding agent. The
result must preserve existing AGENTS.md and tool configuration, work with more
than one agent client, and map generic planning, implementation, and review
activities onto Pyra even when the client does not use Pyra's example skill
names.

## Design

Add a format-aware `internal/agents` planning and rendering package, repeatable
`pyra init --agent` selection, a managed activity-based AGENTS.md block, and
repository-local MCP renderers for Claude Code, Codex, OpenCode, Pi, and Kiro.
Validate and render every affected file before writes, apply individual files
atomically, then dispatch existing and new local hook installers. Pi receives a
project-scoped MCP adapter declaration because its core intentionally has no MCP
client. An `--agents-only` branch applies only the agent plan and bypasses store
configuration resolution, store creation, and hook planning or installation.

## Constraints

Initialization must retain existing store overwrite safety, perform no global
skill installation, preserve unrelated repository configuration, remain
deterministic and idempotent, honor each agent's project trust model, and report
partial multi-file failures honestly. External client formats are versioned
outside Pyra, Pi requires a reviewed third-party adapter, and absolute store
paths must be refreshed by rerunning forced initialization after a checkout is
moved.

## Rationale

TODO

## Alternatives

TODO

## Accessibility

TODO

## Style Guidance

TODO

## Open Questions

TODO

## Related Requirements

- OKF-DEBA7FA1Z6A5

